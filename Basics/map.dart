void main(List<String> args) {
  ///Map Factory
  // Map m = {"1": "one", "2": "two", "3": "three"};
  // m = Map.from({"4": "four"});
  // m = Map.fromEntries([MapEntry("5", "five"), MapEntry("6", "six")]);
  // m = Map.fromIterable(
  //   [1, 2, 3],
  //   key: (element) => element.toString(),
  //   value: (element) => element * element,
  // );
  // print(m);
  //final rings = <bool>[false, false, true, true];
  //final planets = <String>{'Earth', 'Mars', 'Jupiter', 'Saturn'};
  //final map = Map<String, bool>.fromIterables(planets, rings);
  //print(map); // {Earth: false, Mars: false, Jupiter: true, Saturn: true}

  //final planets = <int, String>{1: 'Mercury', 2: 'Venus', 3: 'Earth'};
  //final unmodifiableMap = Map.unmodifiable(planets);
  //unmodifiableMap[4] = 'Mars';

  //var m = <int, int>{1: 1, 2: 2, 3: 3};
  // m[2] = 4;
  // m.update(2, (value) => value * 2);
  //m.updateAll((key, value) => key.isEven ? value * 2 : value + 2);
  //print(m);
}
